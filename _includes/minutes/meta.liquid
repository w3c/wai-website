{% comment %}
Internally referenced by other minutes includes

Inputs:
- group - group ID (i.e. a top-level key in _data/minutes/groups.json)
{% endcomment %}

<details>
  <summary>Display information about these minutes</summary>
  <p>Showing minutes for channel(s):</p>
  <ul>
    {% for channel in site.data.minutes.groups[include.group] -%}
      <li>{{ channel }}</li>
    {%- endfor %}
  </ul>
</details>
